import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "smart cinema & Cinema Franchise Cost in India | Connplex",
  description: "Own a high-profit smart cinema franchise in India. Explore Connplex FOCO/FOFO franchise cost, premium cinema setup investment, and profit models.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
